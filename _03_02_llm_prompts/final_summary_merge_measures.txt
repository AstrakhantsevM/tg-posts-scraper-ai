Регион: {{region}}

Тебе переданы {{batches_total}} промежуточных JSON-ответов одного формата.

ЗАДАЧА:
Объединить их в один итоговый JSON.

ПРАВИЛА СЛИЯНИЯ:
1. Возьми все объекты из всех массивов "measures".
2. Найди дубликаты — меры, которые описывают одно и то же,
   даже если названия чуть отличаются.
3. Из каждой группы дублей оставь одну запись — ту, где больше
   заполненных полей: amount, conditions, notes.
4. Если не уверен, что два объекта — дубли, оставь оба.
5. Верни итоговый JSON строго в формате:
   {"found": true/false, "measures": [...]}

found = true, если measures непустой, иначе false.

ЗАПРЕЩЕНО:
- добавлять меры, которых нет во входных JSON-ах;
- брать данные из примеров;
- менять суммы, условия или форму предоставления;
- возвращать что-либо кроме валидного JSON.